// Temporary dummy test to keep the tests folder non-empty.
// Remove this file once real tests are implemented.

describe('dummy test', () => {
  it('should sum 1 + 2 correctly', () => {
    expect(1 + 2).toBe(3);
  });
});
